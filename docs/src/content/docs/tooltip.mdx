---
title: Tooltip
description: Hover/focus hint with delay and skip-delay across items.
---

import ApiTable from '../../components/ApiTable.svelte';
import TooltipDemo from '../../components/islands/TooltipDemo.svelte';

Hover/focus hint with delay and skip-delay across items.

<TooltipDemo client:visible />

## Anatomy

```svelte title="App.svelte"
<script>
  import { Tooltip } from 'svbase';
</script>

<Tooltip.Root>
  <Tooltip.Trigger>Hover me</Tooltip.Trigger>
  <Tooltip.Content>Helpful hint</Tooltip.Content>
</Tooltip.Root>
```

## API reference

### Tooltip.Root

<ApiTable
	props={[
		{
			name: 'open',
			type: 'boolean',
			defaultValue: '—',
			description: 'Controlled open state. Omit for uncontrolled usage.'
		},
		{
			name: 'placement',
			type: 'Placement',
			defaultValue: "'top'",
			description: 'Preferred placement; flips on viewport collision.'
		},
		{
			name: 'delay',
			type: 'number',
			defaultValue: '600',
			description: 'Hover open delay in ms. Focus opens immediately.'
		},
		{
			name: 'closeDelay',
			type: 'number',
			defaultValue: '0',
			description: 'Close delay in ms after pointer leave.'
		},
		{
			name: 'skipDelayDuration',
			type: 'number',
			defaultValue: '400',
			description: 'Opening skips the delay when another tooltip closed within this window.'
		}
	]}
/>

## Keyboard interactions

- Focus opens immediately; blur and `Esc` close.
- Tooltips never take focus themselves — `role="tooltip"` content is announced via `aria-describedby`.
