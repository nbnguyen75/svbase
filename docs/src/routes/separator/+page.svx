<script lang="ts">
	import { Separator } from 'svbase';

	import ApiTable, { type ApiProp } from '../ApiTable.svelte';
	import CodeBlock from '../CodeBlock.svelte';

	const anatomy = `<script>
  import { Separator } from 'svbase';
<\/script>

<Separator.Separator />`;

	const rootProps: Array<ApiProp> = [
		{
			name: 'orientation',
			type: "'horizontal' | 'vertical'",
			defaultValue: "'horizontal'",
			description: 'Reading direction of the divider.'
		},
		{
			name: 'ref',
			type: 'HTMLDivElement',
			defaultValue: '—',
			description: 'Two-way bound access to the underlying DOM element.'
		}
	];
</script>

<p class="mb-2 text-xs tracking-[0.08em] text-muted-foreground uppercase">Svbase · Primitives</p>

# Separator

Accessible divider with orientation.

<div class="mt-4 rounded-xl border bg-card p-6 text-card-foreground">
	<p>Above</p>
	<Separator.Separator class="my-4 border-t" />
	<p>Below</p>
	<div class="flex items-center gap-3">
		<span>Left</span>
		<Separator.Separator orientation="vertical" class="h-5 border-l" />
		<span>Right</span>
	</div>
	<p class="mt-3 text-sm text-muted-foreground">
		Inspect <code class="rounded-md border bg-muted px-1.5 py-0.5 font-mono text-[0.8em]">role="separator"</code> with <code class="rounded-md border bg-muted px-1.5 py-0.5 font-mono text-[0.8em]">aria-orientation</code>.
	</p>
</div>

## Anatomy

<CodeBlock code={anatomy} filename="App.svelte" />

## API reference

### Separator.Separator

<ApiTable props={rootProps} />

## Keyboard interactions

- None — separators are never focusable.
