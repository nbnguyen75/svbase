<script lang="ts">
	import { Menubar } from 'svbase';

	import ApiTable, { type ApiProp } from '../ApiTable.svelte';
	import CodeBlock from '../CodeBlock.svelte';
	import Kbd from '../Kbd.svelte';

	let picked: string | null = $state(null);

	const anatomy = `<script>
  import { Menubar } from 'svbase';
<\/script>

<Menubar.Root>
  <Menubar.Menu value="file">
    <Menubar.Trigger>File</Menubar.Trigger>
    <Menubar.Content>
      <Menubar.Item>New</Menubar.Item>
    </Menubar.Content>
  </Menubar.Menu>
</Menubar.Root>`;

	const rootProps: Array<ApiProp> = [
		{
			name: 'orientation',
			type: "'horizontal' | 'vertical'",
			defaultValue: "'horizontal'",
			description: 'Arrow-key axis for moving between menus.'
		},
		{
			name: 'disabled',
			type: 'boolean',
			defaultValue: 'false',
			description: 'Ignores interaction across all menus.'
		}
	];
</script>

<p class="mb-2 text-xs tracking-[0.08em] text-muted-foreground uppercase">Svbase · Primitives</p>

# Menubar

Coordinated menu bar: arrows travel across triggers, opening one menu closes the rest.

<div class="mt-4 rounded-xl border bg-card p-6 text-card-foreground">
	<Menubar.Root>
		<Menubar.Menu value="file">
			<Menubar.Trigger>File</Menubar.Trigger>
			<Menubar.Content class="min-w-48 rounded-lg border bg-popover p-1 shadow-md">
				<Menubar.Item
					class="rounded px-2 py-1.5 text-sm data-highlighted:bg-accent"
					onSelect={() => (picked = 'new')}
				>
					New
				</Menubar.Item>
				<Menubar.Item
					class="rounded px-2 py-1.5 text-sm data-highlighted:bg-accent"
					onSelect={() => (picked = 'open')}
				>
					Open
				</Menubar.Item>
			</Menubar.Content>
		</Menubar.Menu>
		<Menubar.Menu value="edit">
			<Menubar.Trigger>Edit</Menubar.Trigger>
			<Menubar.Content class="min-w-48 rounded-lg border bg-popover p-1 shadow-md">
				<Menubar.Item
					class="rounded px-2 py-1.5 text-sm data-highlighted:bg-accent"
					onSelect={() => (picked = 'undo')}
				>
					Undo
				</Menubar.Item>
			</Menubar.Content>
		</Menubar.Menu>
	</Menubar.Root>
	<p class="mt-3 text-sm text-muted-foreground">Picked: {picked ?? '—'} (Tab in, arrows travel, Enter opens)</p>
</div>

## Anatomy

<CodeBlock code={anatomy} filename="App.svelte" />

## API reference

### Menubar.Root

<ApiTable props={rootProps} />

`Menubar.Menu` (unique `value`, optional `disabled`) wraps a dropdown-menu root and bridges its open state; `Menubar.Trigger` is a `menuitem` driver with traversal, hover/focus switching, and explicit activation. Menu contents reuse the dropdown-menu parts unchanged — see [Dropdown Menu](/dropdown-menu).

## Keyboard interactions

- <Kbd>←</Kbd><Kbd>→</Kbd> move focus between triggers (vertical orientation: <Kbd>↑</Kbd><Kbd>↓</Kbd>); <Kbd>Home</Kbd> / <Kbd>End</Kbd> jump.
- <Kbd>Enter</Kbd> / <Kbd>Space</Kbd> / <Kbd>↓</Kbd> opens the focused menu; <Kbd>Esc</Kbd> closes.
- Moving focus or hovering across triggers while a menu is open switches menus.
