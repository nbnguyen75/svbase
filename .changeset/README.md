# Changesets

Add a markdown file here on every user-facing change:

```md
---
'svbase': patch
---

Fix focus leak in Dialog.
```

Use `major` for breaking changes, `minor` for new primitives or props,
`patch` for fixes. The release workflow versions, changelogs, and publishes.
